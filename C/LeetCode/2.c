#include <stdio.h>

typedef struct node
{
    int val;
    struct node *next;
}Node;

typedef Node * NodePtr;

struct NodePtr * R()

struct ListNode* addTwoNumbers(struct ListNode* l1, struct ListNode* l2) 
{
    NodePtr sNodePtr = NULL;

    NodePtr newPte = malloc(sizeof(l1) + sizeof(l2));

    if(l1 && l2)
    {

    }

    else
    {
        return 0;
    }
}